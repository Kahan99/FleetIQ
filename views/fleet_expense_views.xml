<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_fleetflow_expense_tree" model="ir.ui.view">
        <field name="name">fleetflow.expense.tree</field>
        <field name="model">fleetflow.expense</field>
        <field name="arch" type="xml">
            <tree decoration-success="trip_state == 'completed'" decoration-warning="trip_state == 'dispatched'" decoration-info="trip_state == 'draft'" decoration-muted="trip_state == 'cancelled'">
                <field name="vehicle_id"/>
                <field name="trip_id"/>
                <field name="liters"/>
                <field name="fuel_cost"/>
                <field name="cost_per_km"/>
                <field name="expense_date"/>
                <field name="trip_state" optional="hide"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>

    <record id="view_fleetflow_expense_form" model="ir.ui.view">
        <field name="name">fleetflow.expense.form</field>
        <field name="model">fleetflow.expense</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(action_fleetflow_trip)d" type="action" class="oe_stat_button" icon="fa-road" context="{'search_default_id': trip_id}">
                            <div class="o_stat_info"><span class="o_stat_text">Trip</span></div>
                        </button>
                        <button name="%(action_fleetflow_vehicle)d" type="action" class="oe_stat_button" icon="fa-truck" context="{'search_default_id': vehicle_id}">
                            <div class="o_stat_info"><span class="o_stat_text">Vehicle</span></div>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="vehicle_id"/>
                            <field name="trip_id"/>
                            <field name="expense_date"/>
                            <field name="company_id" groups="base.group_multi_company" readonly="1"/>
                        </group>
                        <group>
                            <field name="liters"/>
                            <field name="fuel_cost"/>
                            <field name="cost_per_km" readonly="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_fleetflow_expense_search" model="ir.ui.view">
        <field name="name">fleetflow.expense.search</field>
        <field name="model">fleetflow.expense</field>
        <field name="arch" type="xml">
            <search>
                <field name="vehicle_id"/>
                <field name="trip_id"/>
                <field name="expense_date"/>
                <filter name="trip_draft" string="Trip Draft" domain="[('trip_state', '=', 'draft')]"/>
                <filter name="trip_dispatched" string="Trip Dispatched" domain="[('trip_state', '=', 'dispatched')]"/>
                <filter name="trip_completed" string="Trip Completed" domain="[('trip_state', '=', 'completed')]"/>
                <filter name="trip_cancelled" string="Trip Cancelled" domain="[('trip_state', '=', 'cancelled')]"/>
                <group expand="0" string="Group By">
                    <filter name="group_by_vehicle" string="Vehicle" context="{'group_by': 'vehicle_id'}"/>
                    <filter name="group_by_trip_state" string="Trip Status" context="{'group_by': 'trip_state'}"/>
                    <filter name="group_by_date" string="Date" context="{'group_by': 'expense_date'}"/>
                    <filter name="group_by_company" string="Company" context="{'group_by': 'company_id'}" groups="base.group_multi_company"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
